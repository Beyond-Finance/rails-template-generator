
<% if @template.ui? %>gem 'slim-rails'<% end %>
gem 'aws-sdk'
gem 'aws-sdk-rails'
gem 'aws-sdk-secretsmanager', require: false
gem 'newrelic_rpm'
<% if @template.api? %>gem 'rswag'<% end %>
<% unless @template.api? %>
gem 'route_downcaser'
gem 'uglifier'
<% end %>

